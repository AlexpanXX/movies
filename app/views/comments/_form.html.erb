<%= simple_form_for [@film, @comment] do |f| %>
  <div class="form-group">
    <%= f.input :content, input_html: { class: "form-control" } %>
  </div>
  <div class="form-action">
    <%= f.submit "Submit", data: { disable_with: "Submiting..." }, class: "btn btn-primary" %>
  </div>
<% end %>
